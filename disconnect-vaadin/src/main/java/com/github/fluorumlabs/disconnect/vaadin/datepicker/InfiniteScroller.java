package com.github.fluorumlabs.disconnect.vaadin.datepicker;

import com.github.fluorumlabs.disconnect.core.components.HtmlComponent;
import js.lang.external.vaadin.datepicker.InfiniteScrollerElement;

/**
 * <strong>Mixins:</strong> ElementMixin
 */
class InfiniteScroller extends HtmlComponent<InfiniteScrollerElement> {

    // !wca! get active: boolean

    // !wca! set active: boolean

    // !wca! observe active: boolean

    // !wca! get bufferSize: number

    // !wca! set bufferSize: number

    // !wca! observe bufferSize: number

    // !wca! get itemHeight: number

    // !wca! observe itemHeight: number

}
