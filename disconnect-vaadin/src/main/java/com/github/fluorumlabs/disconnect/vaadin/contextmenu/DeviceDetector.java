package com.github.fluorumlabs.disconnect.vaadin.contextmenu;

import com.github.fluorumlabs.disconnect.core.components.HtmlComponent;
import js.lang.external.vaadin.contextmenu.DeviceDetectorElement;

/**
 * Element for internal use only.
 */
class DeviceDetector extends HtmlComponent<DeviceDetectorElement> {

    // !wca! get phone: boolean

    // !wca! set phone: boolean

    // !wca! get touch: boolean

    // !wca! set touch: boolean

    // !wca! get wide: boolean

    // !wca! set wide: boolean
}
